.tag-cloud {
  text-align: center;

  a {
    display: inline-block;
    margin: 10px;
    color: var(--link-hover-color) !important;

    &:hover {
      color: var(--link-hover-color) !important;
    }
  }
}
